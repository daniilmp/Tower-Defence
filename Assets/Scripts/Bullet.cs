using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bullet : MonoBehaviour
{
    //private Enemy _target;
    //private void Update()
    //{
    //    if (_target != null)
    //    {
    //        if (Vector3.Distance(transform.position, _target.transform.position) > 0.1f)
    //            transform.Translate((_target.transform.position - transform.position).normalized * Time.deltaTime * 10f, Space.World);
    //        else
    //        {

    //            _target.TakeDamage(1);
                
    //            Destroy(gameObject);

    //        }

    //    }else
    //    { Destroy(gameObject); }
    //}
    //public void SetTarget(Enemy targetPosition)
    //{
    //    _target = targetPosition;
    //}
}
